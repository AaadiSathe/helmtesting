apiVersion: v1
kind: Service
metadata:
  name: {{ include "oe.fullname" . }}
  labels:
    {{- include "oe.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "oe.selectorLabels" . | nindent 4 }}
